---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"div">;

const { class: className, ...rest } = Astro.props;
---

<div
  {...rest}
  class:list={["animate-pulse rounded-md bg-grey-100/30", className]}
>
</div>
